其原理类似于等高线，当函数在一个条件的约束下取得极值的时候，应该找的是，梯度和条件本身形成的图形的相同点

方法是，形成一个新的函数

$$F(x,y,\lambda)=f(x,y)+\lambda (ax+by)$$

$$s.t.F_x=0,\quad F_y=0,\quad F_\lambda=0$$

通过解上面的方程，可以得到一系列的点，将点带入、比较。取其中最大的为max，最小的为 min