如果上限是一个函数，那么最终需要多乘以函数的倒数。下限同。上限的倒数减下限的倒数。

最麻烦的情况，形如下式：
$$\int _0^x g(x,u)du$$

解法：
1. 如果是 $g(x,u)$ 来源于另一个积分，意味着这实际是一个二重积分，可以考虑交换积分次序
2. 如果 $g(x,u)=I(x)\times h(u)$ ，那么可以将$I(x)$分离出去，因为这是一个常数
3. 或者需要进行换元[^1]

如果都不能用，解不出来。

[^1]: 换元时候，如果形如$x=sint$上下线需要进行相应更换。