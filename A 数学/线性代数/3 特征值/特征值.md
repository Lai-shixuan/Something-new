依旧是针对方阵，现在来找一些特殊的向量，特征向量

*下节课才讲怎么用这些向量*

矩阵是什么？矩阵就像一个函数，多维函数同时作用于很多个X，而不是仅作用于1个x。尤其是在$Ax=b$方程中。

其中一些特殊的，使$Ax$parrallel to $x$的，就是特征向量(eigenvectors)
$$Ax=\lambda x$$

其中$\lambda$允许为0，或者为负的。

其中比较特殊的一点是，if $A$ is singular, $\lambda=0$ is evalue, x is null space of $A$

# 投影矩阵的特征值

特征矩阵可以有两种特征值
1. 对于任何一个在平面A上的向量x，使用投影矩阵投影到平面A上后，仍然是x，意味着$\lambda=1$
2. 对于任何一个垂直于平面A的向量x，投影到平面A上后，为0，意味着$\lambda=0$

# 置换矩阵的特征值

对于一个换行的矩阵，如果特征向量中，每一个元素都相同，例如[1,1]，那么特征值是1.

对于一个n\*n的方程，拥有n个特征值。且对角线的和 = 特征值的和，称之为迹

# $Ax=\lambda x$

$$rewrite:(A-\lambda I)x=0$$

由于括号内是一个奇异矩阵

$$\therefore det(A-\lambda I)=0$$

*重复的$\lambda$将是本课程的难点*


矩阵+3I，特征值+3，特征向量不变

证明：
![[Pasted image 20210913085452.png]]

*本章的重点就是观察特征值和特征向量*

BUT

![[Pasted image 20210913085859.png]]

补充旋转矩阵，21.21，38min 17s

但旋转矩阵的性质还没有完全说清楚

特征值之积=行列式

有些时候要出现复数特征值，因为无可避免

如果矩阵越对称，那么特征值越趋近于实数。如果越不对称，甚至是反对称的(Q^T=-Q)，那么越可能是虚数

上三角矩阵，特征值就是每一个对角线上的值，证明如下：

![[Pasted image 20210913091435.png]]

对于拥有相同特征值的矩阵，其特征向量少了，没有其他的无关的特征向量了。因此是一个“退化”了的。

which 是很令人讨厌的

# 特征值的性质

特征值没有什么特殊的性质，最好是硬解，或者是猜值。

行变换后均不等于其原始特征值。

方阵转置后，特征值相等