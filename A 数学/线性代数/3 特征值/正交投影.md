# WHY 投影

我们为什么投影，可能因为 Ax=b 没有解。那么，我们求解最接近的方法。

**Solve**：我们求解$A\hat{x}=p$，此时p是b在C(A)上的投影

# 向量的投影
一个向量投影的例子，以及几何图形：

![[Pasted image 20210909122550.png|200]]

解出x的值，于是得到p

$$\because a^T(b-xa)=0$$

$$\therefore \vec{p}=\vec{a}X=\vec{a}\frac{A^Tb}{A^Ta}\tag{1}$$

这个公式告诉我们一个有趣的事情：如果b翻倍，p翻倍；如果a翻倍，p不变

所以，让我们将公式（1）换一个角度理解[^1]：

$$P=\frac{aa^T}{a^Ta},\quad \vec{p}=P\vec{b}$$
其中，这里的P指代的是一个投影矩阵，这个矩阵和 $\vec{b}$ 相乘，可以得到投影向量 $\vec{p}$

关于矩阵P，我们有以下特性：

1. 其几何意义是，$c(P)=$ line through $\vec{a}$（必须通过0点）；
2. 其阶数是：$r(p)=1$
	*这就是一个秩1矩阵！且公式里正好就是，列和行相乘*
3. 且这个矩阵是对称的：证明方法是对其公式进行转置，发现仍然是自己
4. 且还有一个非常重要的公式；$$P^2=P\tag{3}$$

# 高维投影

那么，我们把维度拉高：

首先，如果A是一个方针，是可以化简的，那么意味着A的列空间将充斥整个m维空间

其次，当A是一个普通矩阵，那么其公式应该和之前相似。有待补充

# 应用

Fitting by a line

 


[^1]: 