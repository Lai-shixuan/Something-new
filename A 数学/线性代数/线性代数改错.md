1. 记得代数余子式是有符号的，276
2. #先不学 范的蒙行列式，279
3. #先不学 拉普帕斯行列式，280
4. 是否考虑特殊类型的行列式？[^1]，281
5. 注意行列式的可加性性质，$|A|+|B|\neq|A+B|$，但是行可以相加，282
6. $A^*A=|A|I$，284
7. 行列式的求法，除了运用行列式性质外，也可以运用特征值。$eig(A^2)=\lambda^2$, $eig(2A)=2\lambda$，285
8. 秩1矩阵有以下性质：$A^2=l\times A$，其中 $l=truce=\Sigma a_{ii}$，289
9. 形如$\begin{bmatrix}A&0\\0&B\end{bmatrix}$的，可以用分块矩阵进行解题，290
10. #再做做 291
11. 进行行变换求逆矩阵的时候，记得带回消掉上面若干行，形成最简行变换，292
12. 进行行变换求逆矩阵的时候，对单独一行乘除一个常数是允许的。但是不能进行列变换。292
13. #再做做 293
14. 进行线性相关判断的时候，建议化到最后一行，形成标准的行阶梯型再进行判断，311
15. 进行行阶梯型，对单独一行乘除一个常数时候，若该常数是$n-3$形式，那么需要返回验证$n=3$，309
16. $A+kE\sim B+kE$，用来判断矩阵的秩，307
17. 可逆矩阵不会改变被操作的矩阵的秩，304
18. #再做做 303
19. 当计算量比较大的时候，注意是否能够巧妙利用逆矩阵，302，293
20. 如果矩阵乘法直接猜解，小心某一个矩阵不可逆。因为猜解的过程是用到了逆矩阵的，不可逆则不可以猜解，299
21. 求逆矩阵没有思路的时候，可以用定义。例如，若$A(A-E)=E$，那么 $A^{-1}=A-E$，296
22. 直接法计算逆矩阵：假设此前矩阵为$A=\begin{bmatrix}\alpha_1&\alpha_2\end{bmatrix}$，现在有矩阵$B$，求$B^{-1}$，则可以将B理解成对A的列进行操作，得到了新矩阵$AB=\begin{bmatrix}a\alpha_1&b\alpha_2\end{bmatrix}$，于是我们可以想象$B^{-1}$的功能操作新矩阵的列向量，使得$AB$成为$ABB^{-1}=A$。因此可解。295
23. 特殊矩阵的逆矩阵：$\begin{bmatrix}0&0&a_1\\0&a_2&0\\a_3&0&0\end{bmatrix}^{-1}= \begin{bmatrix}0&0&1/a_3\\0&1/a_2&0\\1/a_1&0&0\end{bmatrix}$
24. 求非齐次的方程的解的时候，给自由变量赋值为 0，而不是 1，2014 年 22 题
25. 小心任意实数，可能导致某一个向量为 0 向量，因此导致线性相关，卷 1


[^1]: [[我的问题 S]]