TARGET DECK: A 数学公式::2 线性代数

如何证明相似? / 相似对角化的充分条件 #flashcard  

**当有 n 个互不相同的特征值**，那么矩阵可以对角化。若两个矩阵的特征值全部相同，且对应位置相同，那么相似。  
**对于有重根的特征值**，如果特征值的重数 = 该特征值下矩阵线性无关的特征向量的个数（即矩阵的自由变量个数），那么矩阵可以对角化。如果两个矩阵都可以对角化，且对应特征值位置相同，则相似。  
<!--ID: 1639661918225-->
