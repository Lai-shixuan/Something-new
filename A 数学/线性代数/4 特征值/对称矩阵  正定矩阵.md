对称矩阵是特殊的，毕竟特征值也能体现

1. 特征值是实数
2. 特征向量是正交的（如果有特征值是重复的，特征向量形成了一个平面？？？可以选出垂直的特征向量）

*对称矩阵是最重要的矩阵*

如何求出一个矩阵有哪些特征值？必须解方程吗？如何判断一个矩阵存在相同的特征值相同？[^1]  
 
 ![[Pasted image 20210913135326.png]]
 
 $$A=Q\Lambda Q^{-1}=Q\Lambda Q^T$$
 
 *意味着，任何一个对称矩阵，可以被分解成为上述矩阵，并且它仍然是对称的*
 
 # 证明，为什么特征值是实数？
 
 一个没有得到证明的结论，为什么复数特征值总是成对出现
 
 PS: 如果有一个复数，总是可以乘以它的共轭复数，来化简
 
 又是一个结论：
 ![[Pasted image 20210913143345.png]]
 
 
 # 正定（对称）矩阵
 
 是一个对称矩阵，且所有的特征值都是正的
 
 并且所有主元也是正的
 
 所有的子行列式也是正的
 
 
 
 
[^1]: [[我的问题 S]]